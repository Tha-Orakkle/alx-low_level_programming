#include "main.h"


char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	int len1, len2;

	len1 = strlen(n1);
	len2 = strlen(n2);

	if (len2 > len1)
	{
		max_len = len2;
		min_len = len1;
	}
	else
	{
		max_len = len1;
		min_len = len2;
	}

	for (i = (max_len - 1); i >= 0; i--)
	{
		




}
